export type Point3D = {
  x: number;
  y: number;
  z: number;
};

export type PlotOptions = {
  width: number;
  height: number;
  chars: string[];
  zBuffer: number[];
  scaleX: number;
  scaleY: number;
};
